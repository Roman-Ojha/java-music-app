package com.example.javarefresher;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        // here first import the module by going to the file and new Module and pick the java or kotlin library
        // after that you can see the java or kotlin library name folder where we have the Class
        // after that you can code the java programming in that MyClass
    }
}